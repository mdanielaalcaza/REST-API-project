/**
 * Aspectos Teoricos:
 * Entorno de ejecucion para JS. se creacion a del motor v8 en google. 
 * al intalarlo, esta el paquete npm, que es el gestor de paquetes de node,
 * tambien puede ejecutar otras tareas como script.
 */

/**
 * npm: al crear un proyecto crea un archivo packaqe.json.
 * formato escrito: . json (js object notation) estructurado por un objeto,
 * con propiedades y valor. Claves son siempre string, se diferencia de JS. 
 */

/**
 * dependencias: se almacenan que dependencia utiliza nuestro proyecto, en el package.json.
 * pero para que exiostan en nuestro proyecto, se necesitan que esten en la carpeta node_modules
 */

/**
 * RELACION CLIENTE - SERVIDOR
 *CLIENTE - REQUEST - SERVIDOR (CON PRFOTOCOLO HPPT)
 *SERVIDOR - RESPONSE - CLIENTE: (CÓD. DE ESTADO DE RESPUESTA)
 */

/**
 * PETICIONES - METODOS HPPT:
 * GET: Va a decir al servidor , responder con la representacion estado de un dato: 
 * envia datos a nuestro cliente
 * POST: ayuda a generar un nuevo cambio en el servidor, crear nuevos datos
 * PUT: modif. algun recurso en el servidor
 * delete: eliminar algun recurso en el servidor
 */

/**
 * CÓD. DE ESTADO DE RESPUESTA: RESPONSE
 * 100: 1 al 100 Representa algo informartivo, responde info al cliente
 * 200: respuesta de exito
 * 300: respuesta de redireccion
 * 400: errores en el cliente
 * 500: errores en el servidor 
 */

/**
 * CRUD: ACRONIMO
 * CREATE:
 * READ:
 * UPDATE:
 * DELETE:
 */

/**
 * BD: MONGO DB CON MONGO ATLAS
 */
